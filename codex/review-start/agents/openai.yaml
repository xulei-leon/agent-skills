interface:
  display_name: "Review Start"
  short_description: "Run dual reviews with Claude fallback"
  default_prompt: "Use $review-start to review a document or code file with opencode and Claude fallback reports."
