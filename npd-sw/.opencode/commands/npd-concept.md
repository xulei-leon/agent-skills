# /npd-concept

## Goal
Complete Phase 0 (Concept) — research first, then charter.

## Workflow
`.opencode/workflows/phase0-concept.md`

## Constraints
- Research must be approved (`Research Approved`) before creating Charter
- Charter must be approved (`Charter Approved`) before entering Phase 1 (Requirement)
- Each document must have `Version: x.y.z` header
- If `.npd-status.json` has `Concept.enable = false`, this command is skipped automatically
